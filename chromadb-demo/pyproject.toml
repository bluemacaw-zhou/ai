[project]
name = "chromadb-demo"
version = "0.1.0"
description = "ChromaDB 向量数据库连接演示"
requires-python = ">=3.12"
dependencies = [
    "numpy>=1.24.0",
    "ai-starter[chromadb,embedding]",
]

[tool.uv]
index-url = "http://10.100.1.27:8688/repository/Pypi-group/simple"
package = false

[tool.uv.sources]
ai-starter = { path = "../ai-starter", editable = true }
