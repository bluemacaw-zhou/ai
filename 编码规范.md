# Python 编码规范

本文档定义了 AI 工作区的 Python 编码规范和最佳实践。

## 目录

- [项目结构规范](#项目结构规范)
- [命名规范](#命名规范)
- [接口实现规范](#接口实现规范)
- [配置管理规范](#配置管理规范)
- [日志规范](#日志规范)

---

## 项目结构规范

### 标准项目结构

所有 Python 项目必须遵循以下标准结构：

```
project-name/              # 项目根目录（仓库名，可以用横线）
├── project_name/          # 包目录（import 名称，必须用下划线）
│   ├── __init__.py
│   ├── module1.py
│   └── module2.py
├── tests/                 # 测试目录（可选）
├── pyproject.toml
├── config.yaml            # 配置文件（gitignore）
└── README.md
```

### 为什么需要包目录？

#### ✅ 正确的结构
```
chromadb-demo/              # 项目根目录
├── chromadb_demo/          # 包目录
│   ├── __init__.py
│   └── vector_operations.py
├── pyproject.toml
└── config.yaml
```

#### ❌ 错误的结构
```
chromadb-demo/
├── vector_operations.py    # ❌ 直接在根目录
├── pyproject.toml
└── config.yaml
```

### 5 个重要原因

1. **避免导入混淆**
   ```python
   # 错误结构：开发时可能导入错误的文件
   # 正确结构：明确导入包内的模块
   from chromadb_demo.vector_operations import VectorOperations
   ```

2. **支持安装和分发**
   - `uv add ./chromadb-demo` 需要知道包在哪
   - 打包时（`uv build`）需要找到包目录
   - 安装后 `import chromadb_demo` 才能工作

3. **测试隔离**
   ```python
   # 测试应该使用安装的包，而不是源码
   from chromadb_demo import VectorOperations  # ✓ 从已安装的包导入
   ```

4. **明确包边界**
   - 清楚知道哪些代码是包的一部分
   - README、配置文件、文档不会被误认为是包

5. **行业标准**
   - 所有主流 Python 项目都这样做
   - requests, flask, django, numpy 等都是这个结构

### 命名约定

| 类型 | 根目录名 | 包目录名 | 示例 |
|------|---------|---------|------|
| 库项目 | `ai-starter` | `ai_starter` | ✓ |
| 应用项目 | `chromadb-demo` | `chromadb_demo` | ✓ |
| 练习项目 | `python-practice` | `python_practice` | ✓ |

**规则：**
- 根目录名可以使用横线 `-`（对文件系统友好）
- 包目录名必须使用下划线 `_`（Python 导入要求）
- 包目录名应与项目名对应

---

## 命名规范

### Python 的"私有"约定

Python 使用下划线前缀来标识内部实现，但这只是**约定**，不是强制的访问控制。

### 1. 单下划线 `_variable` - 内部使用（约定）

**用途：**
- 模块级别的内部变量/函数
- 类的内部属性/方法
- 告诉开发者："这是内部实现，请不要直接使用"

**示例：**
```python
# 模块级私有变量
_loggers = {}                    # 内部缓存
_trace_context = threading.local()  # 内部状态

# 类的私有属性
class Config:
    _instance = None          # 单例实例（内部管理）
    _config_data = {}         # 配置数据（内部存储）
    _loaded = False           # 加载状态（内部标志）

    # 私有方法
    def _ensure_loaded(self):  # 内部方法
        pass

    def _load_from_file(self, path):  # 内部实现
        pass

    # 公开方法
    def get(self, key):       # 公开 API
        pass
```

**特点：**
- 只是**约定**，技术上仍然可以访问
- IDE 会给出警告提示
- `from module import *` 不会导入 `_` 开头的名称
- 适合大多数场景

### 2. 双下划线 `__variable` - 名称改写

**用途：**
- 避免子类意外覆盖父类的属性
- 更强的"私有"保护（但仍可通过改写后的名称访问）

**示例：**
```python
class MyClass:
    __private_var = "secret"  # 会被改写为 _MyClass__private_var

    def __private_method(self):
        pass
```

**特点：**
- Python 会进行**名称改写**（name mangling）
- 改写为 `_ClassName__variable`
- 仍可通过 `obj._MyClass__private_var` 访问
- **不推荐过度使用**，单下划线通常已足够

### 3. 双下划线前后 `__method__` - 魔术方法

**用途：**
- Python 的特殊方法（不要自定义这种格式）

**示例：**
```python
class MyClass:
    def __init__(self):       # 构造函数
        pass

    def __str__(self):        # 字符串表示
        return "MyClass"

    def __len__(self):        # 长度
        return 0
```

### 命名对比表

| Python | Java/C# | 说明 | 推荐使用 |
|--------|---------|------|----------|
| `variable` | `public` | 公开 | ✓ 公开 API |
| `_variable` | `private`（约定） | 内部使用（约定）| ✓ 大多数内部实现 |
| `__variable` | `private`（强制） | 名称改写 | ⚠️ 特殊场景 |
| `__method__` | N/A | 魔术方法 | ✓ Python 内置 |

### 最佳实践

```python
# ✓ 正确示例
class VectorOperations:
    """向量操作类"""

    def __init__(self, db, embedding):
        self.db = db              # ✓ 公开属性
        self.embedding = embedding
        self._cache = {}          # ✓ 内部缓存

    def add_texts(self, texts):   # ✓ 公开方法
        vectors = self._convert_to_vectors(texts)
        return self._store_vectors(vectors)

    def _convert_to_vectors(self, texts):  # ✓ 内部方法
        pass

    def _store_vectors(self, vectors):     # ✓ 内部方法
        pass
```

```python
# ❌ 错误示例
class VectorOperations:
    def __init__(self, db, embedding):
        self.__db = db              # ❌ 不必要的双下划线
        self.__embedding = embedding

    def add_texts(self, texts):
        # ❌ 无法在子类中访问 __db
        pass
```

### 命名规则总结

| 类型 | 规则 | 示例 |
|------|------|------|
| 公开 API | 普通命名 | `get_logger()`, `Config.load()` |
| 内部实现 | 单下划线前缀 | `_loggers`, `_ensure_loaded()` |
| 避免冲突 | 双下划线前缀 | `__private_var`（少用） |
| 魔术方法 | 双下划线前后 | `__init__()`, `__str__()` |
| 常量 | 全大写 | `MAX_RETRIES`, `DEFAULT_PORT` |
| 类名 | 驼峰命名 | `VectorOperations`, `GLMEmbedding` |
| 函数/变量 | 下划线分隔 | `get_logger`, `trace_id` |

---

## 接口实现规范

### 多重继承显式声明

当类需要实现多个框架/接口时，使用**多重继承**显式声明，让接口关系一目了然。

#### 问题场景

当一个类需要同时兼容多个框架（如 LangChain 和 Qwen-Agent）时，如果只继承其中一个框架的基类，另一个接口的实现关系不够清晰：

```python
# ❌ 不够清晰
class CustomChatZhipuAI(BaseChatModel):  # 只能看到 LangChain 接口
    def chat(self, messages): ...  # Qwen-Agent 接口，但继承关系不明确
    def _generate(self, messages): ...  # LangChain 接口
```

#### 解决方案：多重继承显式声明

```python
# 1. 定义接口（使用 ABC）
# langchain_interface.py
class LangChainChatModel(LangChainBaseChatModel, ABC):
    """LangChain ChatModel 接口"""

    @abstractmethod
    def _generate(self, messages, ...): pass

    @abstractmethod
    def _stream(self, messages, ...): pass

# qwen_agent_interface.py
class QwenAgentLLM(ABC):
    """Qwen-Agent LLM 接口"""

    @abstractmethod
    def chat(self, messages, ...): pass

    @property
    @abstractmethod
    def support_multimodal_input(self) -> bool: pass

# 2. 使用多重继承显式声明
class CustomChatZhipuAI(LangChainChatModel, QwenAgentLLM):
    """
    智谱 AI LLM

    实现接口:
    - LangChainChatModel (继承)
      - _generate(): 非流式生成
      - _stream(): 流式生成

    - QwenAgentLLM (继承)
      - chat(): Qwen-Agent chat 接口
      - support_multimodal_input: 是否支持多模态输入
    """
```

#### 为什么这样做？

1. **代码即文档** - 一眼看出类实现了哪些接口
2. **IDE 友好** - 更好的代码补全和类型检查
3. **编译时检查** - 如果不实现抽象方法，实例化时会报错

#### 接口文件命名规范

- **接口定义文件**: `*_interface.py`（如 `langchain_interface.py`, `qwen_agent_interface.py`）
- **接口类命名**:
  - 继承外部框架类时，使用框架名（如 `LangChainChatModel` 继承 `BaseChatModel`）
  - 纯接口定义，使用 `Interface` 后缀（如 `EmbeddingInterface`）

#### 示例：Embedding 接口

```python
# embedding_interface.py - 内部接口定义
class EmbeddingInterface(ABC):
    @abstractmethod
    def get_embedding(self, text: str) -> List[float]: pass

    @abstractmethod
    def get_model_name(self) -> str: pass

    @abstractmethod
    def get_vector_dimension(self) -> int: pass

# langchain_embedding_interface.py - LangChain 接口定义
class LangChainEmbeddingInterface(LangChainEmbeddings, ABC):
    @abstractmethod
    def embed_documents(self, texts: List[str]) -> List[List[float]]: pass

    @abstractmethod
    def embed_query(self, text: str) -> List[float]: pass

# langchain_glm_embedding.py - 实现
class LangChainGLMEmbedding(LangChainEmbeddingInterface, EmbeddingInterface):
    """
    LangChain 兼容的智谱 AI Embedding 适配器

    实现接口:
    - LangChainEmbeddingInterface (继承)
      - embed_documents(): 批量嵌入
      - embed_query(): 单条嵌入

    - EmbeddingInterface (继承)
      - get_embedding(): 获取向量
      - get_model_name(): 模型名称
      - get_vector_dimension(): 向量维度
    """
```

#### Python 继承语法说明

```python
class ChildClass(ParentClass1, ParentClass2, ...):
    pass
```

- 语法类似函数传参，但含义是**继承**
- 支持多重继承，逗号分隔多个父类
- 方法解析顺序 (MRO) 从左到右

这是 Python 的标准写法，与其他语言对比：

| 语言 | 继承语法 |
|------|----------|
| **Python** | `class Child(Parent1, Parent2):` |
| **Java** | `class Child extends Parent implements Interface1, Interface2` |
| **C++** | `class Child : public Parent1, public Parent2` |
| **C#** | `class Child : Parent, Interface1, Interface2` |

#### 适用场景

- **多框架集成** - 类需要同时支持多个框架的接口
- **插件系统** - 类需要实现多个插件接口
- **适配器模式** - 将一个接口转换为另一个接口

---

## 配置管理规范

### 配置文件优于环境变量

**使用 `config.yaml` 代替环境变量：**

```yaml
# config.yaml
api:
  zhipuai:
    key: "your_api_key"
    proxy: "http://proxy:8080"
    verify_ssl: false

database:
  chromadb:
    host: "localhost"
    port: 8000

logging:
  level: "INFO"
  file: "logs/app.log"
```

### 配置加载模式

**单例模式 + 懒加载：**

```python
from ai_starter import Config

# 方式1: 显式加载（可选）
Config.load()

# 方式2: 自动加载（推荐）
# 组件内部会自动调用 Config()，触发懒加载
from ai_starter import GLMEmbedding

embedding = GLMEmbedding()  # 自动加载配置
```

### 配置读取优先级

**参数 > 配置文件 > 默认值**

```python
# 示例：GLMEmbedding 的配置读取
class GLMEmbedding:
    def __init__(self, api_key=None, model=None):
        config = Config()  # 懒加载

        # 参数优先级：函数参数 > 配置文件 > 默认值
        self.api_key = api_key or config.get("api.zhipuai.key")
        self.model = model or config.get("models.embedding.model", "embedding-2")
```

### Git 忽略规则

```gitignore
# 配置文件包含敏感信息，不提交
config.yaml
config.json
config.toml

# 提供配置模板
!config.example.yaml
```

---

## 日志规范

### 标准日志使用

**基础用法：**

```python
from ai_starter import get_logger

logger = get_logger(__name__)

logger.debug("调试信息")      # 开发调试
logger.info("一般信息")       # 重要流程
logger.warning("警告信息")    # 需要注意
logger.error("错误信息")      # 错误但可继续
logger.critical("严重错误")   # 致命错误
logger.exception("异常")      # 异常 + 堆栈
```

### 日志级别配置

**从配置文件读取：**

```yaml
# config.yaml
logging:
  level: "INFO"           # DEBUG, INFO, WARNING, ERROR, CRITICAL
  file: "logs/app.log"    # 可选：日志文件路径
```

**优先级：**
```python
# 代码中指定 > 配置文件 > 默认值（INFO）
logger = get_logger(__name__, level="DEBUG")  # 覆盖配置文件
```

### 链路追踪

**每个线程自动获得 trace_id：**

```python
from ai_starter import get_logger

logger = get_logger(__name__)

# 自动分配 trace_id
logger.info("处理请求")  # 2024-01-15 10:00:00 - [abc-123] - INFO - 处理请求
logger.info("查询数据库") # 2024-01-15 10:00:01 - [abc-123] - INFO - 查询数据库
```

**为函数分配新 trace_id（HTTP、MQ 等场景）：**

```python
from ai_starter import with_trace, get_logger

logger = get_logger(__name__)

@with_trace
def handle_request(user_id):
    logger.info(f"处理用户请求: {user_id}")
    # 业务逻辑...
    return "Success"

# 每次调用自动获得新的 trace_id
handle_request(123)  # trace_id: xxx-111
handle_request(456)  # trace_id: xxx-222
```

**手动控制 trace_id（高级用法）：**

```python
from ai_starter import trace_context, get_logger

logger = get_logger(__name__)

# 场景：从 HTTP 请求头传递 trace_id
incoming_trace_id = request.headers.get("X-Trace-Id")

with trace_context(incoming_trace_id):
    logger.info("使用传入的 trace_id")
    process_request()
```

### 日志格式（固定）

```
%(asctime)s - [%(trace_id)s] - %(levelname)-5s - [%(threadName)s] - %(filename)s:%(funcName)s:%(lineno)d - %(message)s
```

**格式说明：**
- `%(asctime)s` - 时间戳
- `[%(trace_id)s]` - 链路追踪 ID（同一请求/线程共享）
- `%(levelname)-5s` - 日志级别（DEBUG/INFO/WARNING/ERROR/CRITICAL）
- `[%(threadName)s]` - 线程名称
- `%(filename)s:%(funcName)s:%(lineno)d` - 文件名:函数名:行号
- `%(message)s` - 日志消息

**输出示例：**
```
2024-01-15 10:30:45 - [a1b2c3d4-e5f6-4a7b-8c9d] - INFO  - [MainThread] - main.py:handle_request:42 - 处理请求
2024-01-15 10:30:46 - [f9e8d7c6-b5a4-9382-7160] - INFO  - [Thread-1] - worker.py:process:15 - 工作线程处理中
```

### 日志最佳实践

```python
# ✓ 正确示例
logger.info(f"用户登录成功: {user_id}")
logger.warning(f"API 调用缓慢: {endpoint}, 耗时 {duration}ms")
logger.error(f"数据库连接失败: {error}", exc_info=True)

try:
    result = risky_operation()
except Exception as e:
    logger.exception("操作失败")  # 自动记录堆栈

# ❌ 错误示例
logger.info("处理")  # 信息不明确
logger.debug(f"变量值: {huge_data_structure}")  # 避免记录大量数据
print("日志信息")  # 不要使用 print，使用 logger
```

---

## Python 哲学

> **"We are all consenting adults here"**
>
> （我们都是成年人）

Python 的设计哲学：
- 使用**约定**而非强制
- 单下划线 `_` = "请不要直接使用"（君子协定）
- 双下划线 `__` = "你很难意外访问"（技术手段）
- 信任开发者的判断力

---

## 参考资料

- [PEP 8 - Python 代码风格指南](https://peps.python.org/pep-0008/)
- [Python Logging 文档](https://docs.python.org/3/library/logging.html)
- [Python 包结构最佳实践](https://docs.python.org/3/tutorial/modules.html#packages)
- [uv 文档](https://docs.astral.sh/uv/)

---

## 更新日志

- 2024-01-15: 初始版本，定义项目结构和命名规范
- 2026-01-19: 添加接口实现规范 - 多重继承显式声明
