# Python 编码规范

本文档定义了 AI 工作区的 Python 编码规范和最佳实践。

**完整示例代码：** 参见 `python-practice/python_practice/` 目录

## 目录

- [项目结构规范](#项目结构规范)
- [命名规范](#命名规范)
- [类型注解规范](#类型注解规范)
- [属性封装规范](#属性封装规范)
- [接口实现规范](#接口实现规范)
- [配置管理规范](#配置管理规范)
- [日志规范](#日志规范)

---

## 项目结构规范

### 标准项目结构（必需）

```
project-name/              # 项目根目录（可用横线 -）
├── project_name/          # 包目录（必须用下划线 _）
│   ├── __init__.py
│   └── module.py
├── pyproject.toml
├── config.yaml            # gitignored
└── README.md
```

**命名规则：**
- 根目录名：可用横线 `-`（如 `chromadb-demo`）
- 包目录名：必须用下划线 `_`（如 `chromadb_demo`）

**为什么需要包目录？**
- 支持 `uv build` 打包
- 导入明确：`from chromadb_demo import Module`
- 行业标准（requests, flask, django 等）

---

## 命名规范

### 私有约定

| 命名方式 | 含义 | 用途 |
|---------|------|------|
| `variable` | 公开 | 公开 API |
| `_variable` | 内部使用（约定） | 私有属性/方法（推荐） |
| `__variable` | 名称改写 | 避免子类覆盖（少用） |
| `__method__` | 魔术方法 | Python 内置（如 `__init__`） |

**示例：**
```python
class Config:
    _instance = None          # ✓ 单例实例（内部）
    _config_data = {}         # ✓ 配置数据（内部）

    def _load_from_file(self):  # ✓ 私有方法
        pass

    def get(self, key):         # ✓ 公开方法
        pass
```

**完整示例：** `python-practice/python_practice/naming_example.py`

---

## 类型注解规范

### 核心规则

**所有公开方法和 `@property` 必须显式声明返回值类型**

```python
# ✓ 正确
def create_llm(self, model: str | None = None) -> OpenAILike:
    return OpenAILike(model=model)

@property
def table_infos(self) -> list[TableInfo]:
    return self._table_infos

def __init__(self) -> None:  # 构造函数也要注解
    self._data = []

# ✗ 错误
def create_llm(self, model=None):  # 缺少返回类型
    return OpenAILike(model=model)

@property
def table_infos(self):  # 缺少返回类型
    return self._table_infos
```

### 常见类型

```python
from typing import Optional

def get_name(self) -> str: ...              # 字符串
def get_age(self) -> int: ...               # 整数
def get_items(self) -> list[str]: ...       # 列表
def get_config(self) -> dict[str, int]: ... # 字典
def find_user(self) -> User | None: ...     # 可选类型
def get_pair(self) -> tuple[str, int]: ...  # 元组
```

**完整示例：** `python-practice/python_practice/type_annotation_example.py`

---

## 属性封装规范

### 核心规则

**使用 `@property` 而非 Java 风格的 `get_xxx()` / `set_xxx()` 方法**

```python
# ✓ 正确：Python 风格
class Person:
    def __init__(self, age: int):
        self._age = age

    @property
    def age(self) -> int:
        """获取年龄"""
        return self._age

    @age.setter
    def age(self, value: int) -> None:
        """设置年龄（带验证）"""
        if value < 0:
            raise ValueError("Age cannot be negative")
        self._age = value

# 使用
person = Person(25)
print(person.age)    # ✓ 简洁
person.age = 30      # ✓ 简洁

# ✗ 错误：Java 风格
class Person:
    def get_age(self) -> int:     # ✗ 不推荐
        return self._age

    def set_age(self, value: int) -> None:  # ✗ 不推荐
        self._age = value
```

### 属性类型

| 类型 | 实现 | 说明 |
|------|------|------|
| **只读** | `@property` | 无 setter，外部只能读 |
| **读写** | `@property` + `@xxx.setter` | setter 中可验证 |
| **计算** | `@property`（实时计算） | 不存储，每次计算 |
| **延迟加载** | `@property` + 缓存检查 | 首次访问时加载 |

**完整示例：** `python-practice/python_practice/property_example.py`

---

## 接口实现规范

### 核心规则

**当类需要实现多个框架/接口时，使用多重继承显式声明**

```python
from abc import ABC, abstractmethod

# 1. 定义接口
class LangChainChatModel(ABC):
    @abstractmethod
    def _generate(self, messages): pass

class QwenAgentLLM(ABC):
    @abstractmethod
    def chat(self, messages): pass

# 2. 多重继承显式声明
class CustomChatZhipuAI(LangChainChatModel, QwenAgentLLM):
    """
    实现接口:
    - LangChainChatModel: _generate()
    - QwenAgentLLM: chat()
    """
    def _generate(self, messages): ...
    def chat(self, messages): ...
```

**优势：**
- 代码即文档，一眼看出实现了哪些接口
- IDE 友好，更好的代码补全和类型检查
- 编译时检查，缺少抽象方法时报错

**接口文件命名：**
- `*_interface.py` - 接口定义文件
- 接口类名以 `Interface` 结尾或使用框架名

**完整示例：** `ai-starter/ai_starter/llm/` 和 `ai-starter/ai_starter/embedding/`

---

## 配置管理规范

### 核心规则

**使用 `config.yaml` 而非环境变量**

```python
from ai_starter import Config

# 自动从项目根目录加载 config.yaml
config = Config()  # 懒加载
api_key = config.get("api.zhipuai.key")
model = config.get("models.llm.model", "glm-4-flash")  # 带默认值
```

### 配置文件结构

```yaml
api:
  zhipuai:
    key: "your_api_key"
    verify_ssl: false
    use_proxy: true

models:
  llm:
    model: "glm-4-flash"
  embedding:
    model: "embedding-3"

database:
  chromadb:
    host: "localhost"
    port: 8000

logging:
  level: "INFO"
  file: "logs/app.log"
```

### 配置读取优先级

**参数 > 配置文件 > 默认值**

```python
class LLMFactory:
    @staticmethod
    def create_llm(model: str | None = None) -> OpenAILike:
        config = Config()
        # 参数优先，其次配置文件，最后默认值
        model = model or config.get("models.llm.model", "glm-4-flash")
        ...
```

### Git 忽略规则

```gitignore
# 配置文件包含敏感信息，不提交
config.yaml

# 提供配置模板
!config.example.yaml
```

---

## 日志规范

### 核心规则

**使用 `get_logger()` 而非 `print()`**

```python
from ai_starter import get_logger

logger = get_logger(__name__)

# ✓ 正确
logger.debug("调试信息")      # 开发调试
logger.info("一般信息")       # 重要流程
logger.warning("警告信息")    # 需要注意
logger.error("错误信息")      # 错误但可继续
logger.exception("异常")      # 异常 + 堆栈

# ✗ 错误
print("日志信息")  # ✗ 禁止使用 print()
```

### 日志级别配置

```yaml
# config.yaml
logging:
  level: "INFO"  # DEBUG, INFO, WARNING, ERROR, CRITICAL
  file: "logs/app.log"
```

### 链路追踪

**每个线程自动获得独立 trace_id：**

```python
from ai_starter import get_logger, with_trace

logger = get_logger(__name__)

@with_trace  # 为每次调用分配新 trace_id
def handle_request(user_id: int):
    logger.info(f"处理请求: {user_id}")  # [trace-id-123] - 处理请求
    return "Success"
```

### 日志格式（固定）

```
%(asctime)s - [%(trace_id)s] - %(levelname)-5s - [%(threadName)s] - %(filename)s:%(funcName)s:%(lineno)d - %(message)s
```

**输出示例：**
```
2024-01-15 10:30:45 - [a1b2c3d4] - INFO  - [MainThread] - main.py:handle_request:42 - 处理请求
```

**完整示例：** `python-practice/python_practice/logging_usage_example.py`

---

## 最佳实践总结

| 规范 | 要求 | 示例位置 |
|------|------|----------|
| **项目结构** | 必须有包目录 | 所有项目 |
| **命名规范** | 私有用 `_`，公开用普通命名 | `python-practice/python_practice/naming_example.py` |
| **类型注解** | 所有公开方法/属性必须注解 | `python-practice/python_practice/type_annotation_example.py` |
| **属性封装** | 使用 `@property` | `python-practice/python_practice/property_example.py` |
| **接口实现** | 多重继承显式声明 | `ai-starter/ai_starter/llm/` |
| **配置管理** | 使用 `Config` 类 | `python-practice/python_practice/config_usage_example.py` |
| **日志规范** | 使用 `get_logger()` | `python-practice/python_practice/logging_usage_example.py` |

---

## Python 哲学

> **"We are all consenting adults here"**

Python 的设计哲学：
- 使用**约定**而非强制
- 单下划线 `_` = "请不要直接使用"（约定）
- 双下划线 `__` = "你很难意外访问"（技术手段）
- 信任开发者的判断力

---

## 参考资料

- [PEP 8 - Python 代码风格指南](https://peps.python.org/pep-0008/)
- [Python Logging 文档](https://docs.python.org/3/library/logging.html)
- [Python 包结构最佳实践](https://docs.python.org/3/tutorial/modules.html#packages)

---

## 更新日志

- 2024-01-15: 初始版本
- 2026-01-19: 添加接口实现规范
- 2026-02-04: 添加属性封装规范
- 2026-02-28: 添加类型注解规范
- 2026-02-28: 精简代码示例，引用 python-practice 完整实现
